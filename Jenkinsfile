pipeline {
     agent any
     stages {        
        stage('Build') {
            steps {
               echo " Hello World"
               sh 'mvn clean compile'
                    //python setup.py build // Build the project
                }
            }
        }    
    }
}
